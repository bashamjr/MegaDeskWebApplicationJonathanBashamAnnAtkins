function UpdateQuote()
{

    //milkjugs
    var name = document.getElementById("name").value;
    var date;
    var rushOption;
    var deskWidth;
    var deskDepth;
    var drawerCount;
    var surfaceMaterial;
    var quotePrice = 0M;//not from table, calculated here
    //Note: javascrip can access any element on a page that has an id so input boxes are accessible
    //      in the .js file
   
    //input is row date for row at a quoteid 


    //conversion, isolate, row.rushoption, row.deskwidth, row.drawercount, row.surfaceMaterial
    //use conditionals to validate values inside of row at each location and calculate 
    //cost of desk


    //output
    //result is ,document.getElementById("outPut").innerHtml = "$" + quotePrice + " Is Quote";

}